"""
- Find string in list
	Write a program that checks the string ​​in a list and displays `Found` is string equal `green`.
	Given:
		lsts = ['red', 'green', 'blue']
	Output:
		found
"""

lsts = ['red', 'green', 'blue']
for lst in lsts:
	if lst == 'green':
		print('found')
 